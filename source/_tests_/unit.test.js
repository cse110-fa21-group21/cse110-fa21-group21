// unit.test.js

//Just added functions from main.js. Add and test other functions from other js files! (e.g. ../assets/components/RecipeCard.js)
const functions = require('../assets/scripts/main.js');

// Example
test('000-000-0000 is valid', () => {
    //expect(functions.isPhoneNumber('000-000-0000')).toBe(true);
});
